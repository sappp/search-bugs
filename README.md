### Built for techSee home assignment

React appliction bootstrapped with [Create React App](https://github.com/facebook/create-react-app).
using react, redux, selectors(reselect) and bootstrap(only css)